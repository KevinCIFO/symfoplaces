{% extends 'email.html.twig' %}
{% block title %}Restablece tu contraseña{% endblock %}
{% block main %}
    <div class="m-3 p-3 bg-white">
        <h4>Restablecer la clave de Symfofilms</h4>

        <p>Para restablecer tu contraseña, haz clic en el siguiente botón: </p>
        <div class="text-center p-3 m-3">
            <a class="btn btn-primary" href="{{ url('app_reset_password', {token: resetToken.token}) }}">Cambiar mi contraseña</a>
        </div>
        <p>Si no te funcionara, haz clic en el enlace o cópialo y pégalo en tu navegador.</p>
        <a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>
        <p>Este enlace expirará en {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>
        <p>Saludos de parte del equipo de Symfofilms</p>
    </div>
{% endblock %}